// 1. Parametre olarak gecilen bir array'in kpyyasini return eden bir function yaz
//test: console.log(copyArray([1, 2, 3])) 
// su olmali [1, 2, 3] 

// 2. Parametre olarak gecilen bir array'in ilk item'ini return eden bir function yaz
// test: console.log(getFirst([1, 2, 3]))
// su olmali 1

// 3. Parametre olarak gecilen bir array'in son item'ini return eden bir function yaz
// test: console.log(getLast([1, 2, 3]))
// su olmali 3

// 4. Parametre olarak gecilen bir array'in n'inci item'ini return eden bir function yaz. Ikinci parammetresi n olsun
// test: console.log(getNth([1, 2, 3], 1))
// su olmali 1

// 5. renkler isimli bir array olustur ve icinde sunlar olsun 'kirmizi, 'sari', 'mavi', 'mor'

// 6. renkler'in sonuna 'ultraviyole'yi ekle

// 7. renkler'in basina 'kizilotesi'ni ekle

// 8. butun renkleri tek tek console.log eden bir forEach yaz

// 9. sayfadaki li'leri sec ve bir array'e cevir. buna calendar diye bir isim ver

// 10. calendar'daki tum gunleri tek tek console.log yap

// 11. longDayNames adinda bir obje olustur. key olarak kisa day isimleri (wed) olsun, property olarak da uzun ('wednesday')

// 12. longDayNames.wed'i dene

// 13. longDayNames['wed']'i dene, Sinan bunu aciklasin

// 14. longDayNames[calendar[0]]'i dene. Neden calismadigini dusun. toLowerCase kullanarak sorunu coz.

// 15. calendar icin yeni bir forEach yaz. eger elementlerden birinde meeting class'i varsa, `you have a meeting on x` diye console.log yap

// 16. simdi forEach'de console.log'dan once (eger meeting varsa) .assistant'a visible class'ini ekle ve az once console.log yaptigin degeri log yapmak yerine .assistant-text'in innerHTML'i olarak degistir

// 17. innerHTMl'de kullandigin yaziyi biraz daha iyilestirelim. gun ismini direkt calendar'dan almak yerine 13'de yaptigimiz gibi longDayNames'den uzun ismini al.

// 18. forEach'e bir condition daha ekle. eger off-day class'i varsa, yazinin sonuna 'You have an off-day on x' yazdir. x yine longDayNames'den gelsin

// 19. 'You have an off-day on Saturday. You have an off-day on Sunday.' kismini nasil 'You have off-days on Saturday and Sunday' yapabilecegini dusun.

